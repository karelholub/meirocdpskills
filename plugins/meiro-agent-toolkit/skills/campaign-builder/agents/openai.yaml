interface:
  display_name: "Campaign Builder"
  short_description: "Draft offline campaigns with approval gates"
  default_prompt: "Use $meiro-agent-toolkit:campaign-builder to create an offline campaign proposal without writing to Meiro."
